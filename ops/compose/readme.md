# docker-compose pour dev local (Postgres, Mong...oDB, Redis, RabbitMQ, Minio, Kafka/Redpanda, MinIO(S3), MLflow, Redis, Grafana)
# Ce fichier docker-compose.yml permet de lancer plusieurs services pour le développement local.
# # Pour lancer les services, utilisez la commande suivante :

# docker-compose up -d
# docker-compose.yml
